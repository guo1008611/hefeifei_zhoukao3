eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:10086/eureka/
instance:
  prefer-ip-address: true
server:
  port: 8888
zuul:
  routes:
    user:
      path: /api/**
      serviceId: hetong-service